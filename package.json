{
  "name": "cans-plugin-modal-store",
  "version": "0.0.0",
  "description": "cans plugin for creating modal stores",
  "main": "dist/cansPluginModalStore.common.js",
  "scripts": {
    "test": "mocha test/",
    "dist": "bili --format cjs --compress --browser --name \"cansPluginModalStore\""
  },
  "devDependencies": {
    "bili": "^0.14.0",
    "cans": "^0.0.7",
    "mocha": "^3.2.0",
    "power-assert": "^1.4.2"
  }
}
